# Copy to .env if you prefer env vars over the web UI / config file.
# Create a token in Quercus: Account → Settings → New Access Token.

QUERCUS_CANVAS_URL=https://q.utoronto.ca
QUERCUS_TOKEN=
QUERCUS_DOWNLOAD_DIR=downloads
